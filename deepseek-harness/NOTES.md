# 教学工作记录

## 已确认

- 2026-09-20：目标是了解 DeepSeek Harness 的原理机制，后续开发插件和二次开发；特别要求考虑持续更新。
- 起点：用户能读代码，但 agent 工具调用循环不熟。
- 当前学习目录：`D:/learning-lessons/deepseek-harness`。
- 当前源码目录：`D:/programming/workspace/deepseek-harness`。
- 其他学科记录里的 F: 盘迁移信息不代表本任务环境；以本次用户给出的目录为准。

## 教学决策

- 第一课只完成“解释一个工具调用为什么需要下一次模型请求”，以正常、单工具路径切入。
- 复用已有 pi 课程的样式、选择题与步进器组件，复制到本学科 assets，避免修改既有课程。
- 课程中的源码网页链接固定到提交；更新后的本地源码可能与历史课程不同。
- 日志提交不等于逐事件同步落盘；工具调用、持久化刷新和模型流需要逐层解释。
- `ctx.tools.register()` 已管理注册 effect；不要把显式 `ctx.effect()` 教成所有 register 外必须再套的模板。
- 生成课程和答对浏览器练习都不自动代表通过考核；用户的解释才是学习证据。
- 用户未要求定时监控。更新检查在继续教学时或按需运行，不创建后台任务，不自动拉取仓库。
- 每次新课先执行 `node deepseek-harness/tools/check-source.mjs`，检查本课相关文件；读更新后的文档和实现，再决定是否需要复习。
- 本次只建立路线和第一课。后续课按回答和实际需求生成，避免提前堆出大量可能过期的内容。

## 当前进度

- 第一课已提供，待学习与解释；下一课暂定 Cordis 的 apply / inject / effect。
- 尚未考核任何 Harness 概念；未运行模型请求或真实插件。
- 时间预算、目标插件的具体业务用途尚未确认，可在进入插件实践时再问。

## 本次资料验证（不是学习成绩）

- 检查了三张 HTML 的 27 个本地链接和 28 个源码文件链接。
- 用无浏览器的 DOM 检查验证 8 步演示、边界按钮、重置、两道选择题反馈及选项等长、回忆卡展开和收起。
- 实际运行源码检查脚本：关注范围无变化，29 个关键文件哈希一致。
- 内置浏览器安全策略阻止打开本地 file URL，未完成浏览器视觉检查；未尝试绕过。课程文件可由用户自行打开。
