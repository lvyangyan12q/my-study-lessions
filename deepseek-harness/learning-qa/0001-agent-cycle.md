# 第一课：agent 循环

状态：待回答。答案和评分不会因为课程已生成而自动补齐。

1. 用户让 agent 读一个文件。模型第一步发出工具调用，工具已经成功返回文本。为什么通常还需要第二步模型请求？请写出执行顺序与原因。
2. 某插件只监听事件、记录耗时，没有注册工具。它还是插件吗？模型能直接调用它吗？请分别说明依据。
3. 如果某条信息必须影响下一次模型请求，能否只放进 UI 状态而不进入会话记录？为什么？

复习时不用背路径。能说明谁负责什么，以及数据为何必须经过那一层，就达到了本课目标。

## 用户回答

待用户作答。

## 批改与强化

待回答后填写。下一课开始时用不同情境复测“工具执行成功”和“turn 结束”的区别。
