Status: superseded by LR-0007（工作区已迁 F:/lessons-study/pi，本记录的机器事实与路径已过时，起点/时间预算/社区等确认仍有效）
# 起点与约束已确认

用户在开课前的配置问答中确认了四件事，它们共同决定后续每节课的形状。

## 1. 学习起点（已确认）

- **能读 TypeScript**：看得懂类型标注、接口、泛型语法。
- **没写过 Node 项目**：因此还没有踩过 ESM 与 CommonJS、`package.json` 的 `type` 字段、`.mjs` 后缀、`tsc` 与构建链、`process.argv`、以及异步迭代（`for await`）这些坑。

**含义**：能读懂 `packages/agent/src/agent-loop.ts` 的语法，但"为什么文件后缀是 `.ts` / 为什么 import 要写 `.ts`"这类问题会成为噪音。课程里的代码先只用 `node` 直接跑的 `.mjs`，不引入构建链。Node 基础顺路在真实代码里补，不单独开课。

## 2. 时间预算（已确认）

每天 1 小时，约每周 7 小时。

**含义**：一节课的内容量必须能被"读 + 动手"在 60 分钟内吃完。宁可短。练习必须是单文件、单命令、能自动判定成败的。

## 3. 社区（已确认）

愿意加入 pi 的 Discord。因此后续课程可以推荐把问题带到社区去问，这属于"智慧"那一层，不必绕开。

## 4. 仓库形态（已确认，后续有变更）

学习材料独立成一个 git 仓库，根目录 `D:/learning-lessons`，每门学科一个子目录，`pi/` 是第一门。仓库推到 GitHub 的 `my-study-lessions`。

## 待验证

第 1 课的练习（`exercises/0001-agent-loop.mjs`）还没跑。它的输出是校准 ZPD 的第一份证据：如果循环结构写不出来，第 2 课要先补"状态与消息形状"；如果轻松通过，第 2 课可以直接进入"工具失败的四种情形"。
