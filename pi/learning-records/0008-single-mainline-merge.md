# 2026-09-20：双线合并为单一主线（二次开发 + 手搓 agent）

用户随后决定：不要再分主线/支线。课程必须一条主线同时覆盖「Pi 二次开发」与「手搓 agent」两个目标。

## 做了什么

- `LEARNING_PATH.md` 重写为单一顺序主线（阶段 0–15）：
  - 阶段 0–1：认清发布包 + 作为用户掌握主流程（原样保留）。
  - **阶段 2：Agent 最小形态**——旧「手搓」内容正式进主线：0003 课（7 行循环）、练习 0001、`packages/agent/README.md`、`agent-loop.ts` 的 `runLoop()`、0004 课（工具失败）、练习 0002、术语表。放在扩展知识之前，作为后面一切的地基（模型无状态、messages 数组、compaction 的由来）。
  - 阶段 3–14：原扩展/SDK/RPC/TUI/Provider/复杂示例全部保留，但每个阶段新增「复盘（贯通）」项：把新学的扩展事件/StreamFn/compaction/SDK 会话/工具执行边界映射回真实循环的哪个点。
  - **阶段 15：手搓 agent 毕业任务**——≤150 行 TypeScript CLI agent：结构抄阶段 2 骨架；必须处理工具四种失败与上下文超长；支持流式 emit 与 Ctrl+C 出口；先用 faux provider（`packages/ai/src/providers/faux.ts`）跑通，再换真实模型；交付附 1 页「哪里简化了 pi、哪些工程必须 pi 这种规模」的对照说明，并回答「该放扩展层还是核心层」。
- 课程编号体系不变（0001–0004 已是连续主序列，后续 0005 起）；0003/0004 的 kicker 与交叉链接改为「阶段 2 第 N 课」口径。
- 元文件同步：MISSION（约束改为主线包含 packages/agent）、NOTES、AGENTS.md（双线 → 单一主线）、父 README。

## 关键洞察

- 合并的粘合点是「循环」：扩展事件钩子 = 循环里的 emit 点；SDK 会话 = 同一个循环的托管入口；compaction/摘要 = 循环对 messages 生长的应对；工具执行边界 = 扩展层能替换的最大一件事。每阶段一条「回到循环」的复盘项，让手搓目标全程在场，而不是事后补。
- 毕业任务刻意「不 import pi」：参照理解而非复用代码，才配叫手搓；验收用假模型先行，避免 API key 门槛。
- 阶段 2 提前读内核一小块（agent 循环）是权衡：它足够小（7 行骨架）、且是后续每块扩展知识的解释根。其余内核精读仍推迟到毕业后。

Status: active